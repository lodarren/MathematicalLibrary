import org.junit.jupiter.api.Test;
import persistence.JsonReader;

public class JsonReaderTest {
    @Test
    void testReaderNoLibraryTest() {
        JsonReader reader = new JsonReader("./data/doesnotexist.json");
        try {
            Library
        }
    }
}
